# 요청 문구 모음

이 저장소에는 `CLAUDE.md` 와 `SPEC.md` 가 있어서 **Claude Code 가 규약을 자동으로 읽는다.**
그래서 매번 긴 프롬프트를 붙여넣을 필요가 없다. 아래 짧은 문구면 충분하다.

> 실행 위치: `ai-curriculum-web/` 안에서 `claude` 를 실행해야 `CLAUDE.md` 가 자동 로드된다.
>
> **작업 방식**: main 에서 바로 작업하고, `check.mjs` 가 통과하면 바로 push 한다.
> 브랜치·PR 을 만들지 않고 "머지할까요?"라고 되묻지도 않는다.
> 따라서 아래 문구 하나면 배포까지 끝난다.

---

## 1. 기본 — 절 하나 만들기

```
2.3 절 만들어줘.
```

이것만으로 동작한다. 더 얹고 싶다면:

```
2.3 절 만들어줘.
- 벡터를 마우스로 끌면 내적·각도·코사인 유사도가 같이 변하는 위젯을 꼭 넣어줘.
- 임베딩 검색에서 왜 코사인을 쓰는지도 다뤄줘.
```

---

## 2. 여러 절 한 번에

```
2.1, 2.2, 2.3 절 만들어줘.
```

한 번에 3~4절이 적당하다. 그 이상은 품질이 떨어지고 검토도 어렵다.

---

## 3. 장 통째로

```
2장(선형대수) 전체를 만들어줘. 절 하나씩 순서대로 진행하고,
각 절이 끝날 때마다 check.mjs 돌려서 통과하는지 확인해줘.
```

10개 절이면 시간이 오래 걸린다. 백그라운드로 돌리는 편이 낫다.

---

## 4. 이미 만든 절 보강

```
2.3 절에서 '정사영' 설명이 약해. 그림 하나 더 넣고 수치 예제도 추가해줘.
```

```
12.3 절 위젯이 모바일에서 잘려. 고쳐줘.
```

---

## 5. 난이도·분량 조절

```
앞으로 만드는 절은 유도 과정을 더 자세히 써줘. 한 줄씩 건너뛰지 말고.
```

```
5장은 이론보다 '언제 어떤 걸 쓰는지' 판단 기준을 강조해줘.
```

이런 지시는 일회성이 아니라 계속 적용하고 싶다면 `SPEC.md` 에 반영해 달라고 하면 된다.

```
방금 말한 기준을 SPEC.md 에 반영해서 앞으로도 지켜지게 해줘.
```

---

## 5b. 출처

절을 만들 때 출처는 자동으로 붙는다(`SOURCES.md` 규약: 4개 이상, 유형 3종 이상).
따로 챙기고 싶을 때:

```
12.3 절 만들 때 원논문은 꼭 받아서 읽고 써줘. 인용에 어느 절을 참고했는지도 적어줘.
```

```
2장에 필요한 교재들 미리 다 받아둬.
```

```
등록된 출처 링크 중에 죽은 게 있는지 확인해줘.
```
→ `node tools/refs.mjs verify`

```
지금까지 쓴 절들 출처가 논문에만 쏠려 있지 않은지 봐줘.
```
→ `node tools/refs.mjs report`

받은 원문은 `references/library/` 에 쌓인다(gitignore 대상이라 공개되지 않음).
다른 기기에서는 `node tools/refs.mjs fetch` 한 번으로 같은 파일을 복원할 수 있다.

---

## 6. 점검

```
지금까지 만든 절들 스타일이 서로 어긋난 데 없는지 확인해줘.
```

```
check.mjs 돌려서 오류 있으면 고쳐줘.
```

```
2.3 절을 모바일 375px 폭이랑 다크모드에서 확인해줘.
```

---

## 7. 커리큘럼 목차 자체를 고칠 때

```
curriculum/ai_curriculum_v2.md 의 12.8 절에 'Jamba' 항목 추가하고,
curriculum-data.js 다시 빌드해줘. 이미 만든 페이지 중 영향받는 게 있으면 알려줘.
```

---

## 8. 배포 문제

```
배포된 사이트에서 수식이 안 보여. 원인 찾아줘.
```

```
GitHub Pages 에 반영이 안 된 것 같아. 확인해줘.
```

---

## 권장 진행 순서

347개 절은 장기전이다. 의존성 순서대로 쌓는 편이 이해가 빠르다.

1. **2장 선형대수** — 이후 모든 장의 언어
2. **3장 미적분과 최적화** — 학습이 어떻게 일어나는가
3. **4장 확률과 통계** — 손실함수와 생성 모델의 근거
4. **6장 딥러닝 기초** — 여기까지가 토대
5. 그다음은 관심 주제 우선 (7장 비전 / 12장 Transformer / 13장 LLM …)

1장(AI 개론)은 개념 나열이라 나중에 훑어도 된다.

---

## 처음 한 번만 — 형식 확정

아직 절을 하나도 만들지 않았다면 이렇게 시작하는 것을 권한다.

```
2.3 절을 만들어줘. 이게 첫 번째 절이니까, 만든 다음에
"이 형식을 347개 절에 그대로 적용해도 괜찮을지" 스스로 점검하고
고칠 만한 점이 있으면 같이 알려줘.
```

만들어진 결과를 보고 마음에 안 드는 부분을 말하면, 그 피드백이 `SPEC.md` 에 반영되어
이후 모든 절에 자동으로 적용된다.
